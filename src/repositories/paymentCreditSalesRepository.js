import { prisma } from "../../server.js"

export default class PaymentCreditSalesRepository{

    // async createPaymentCreditSale(data) {
    //     return await prisma.paymentCreditSale.create({
    //         data : {
    //             data.
    //         }
    //     })
    // }
}